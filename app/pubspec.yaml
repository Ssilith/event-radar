name: app
description: "Event Radar"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  json_annotation: ^4.11.0

  diacritic: ^0.1.6

  http: ^1.6.0

  flutter_native_splash: ^2.4.7
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  json_serializable: ^6.13.0
  build_runner: ^2.12.2

flutter:
  uses-material-design: true

  assets:
    - assets/
